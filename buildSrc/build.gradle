buildscript {
    repositories {
        jcenter()
    }
    dependencies {
        classpath("org.jetbrains.kotlin:kotlin-gradle-plugin:1.3.72")
    }
}

apply(plugin: "kotlin")

repositories {
    jcenter()
}

sourceSets {
    main {
        java.srcDir("src/main/kotlin")
    }
}
