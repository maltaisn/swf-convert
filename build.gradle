buildscript {
    repositories {
        jcenter()
        mavenCentral()
    }
    dependencies {
        classpath("org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlinVersion")
    }
}

plugins {
    id "base"
}

allprojects {
    repositories {
        jcenter()
        mavenCentral()
    }
}

tasks.clean {
    delete(buildDir)
}
